# Sample ISO 8583 bodies (Switch → IMPS)

Use these binary files when calling the **Switch - IMPS flow** APIs if the collection’s pre-request script (base64 → body) does not send correct binary in your Postman version.

| File | MTI | Use for |
|------|-----|--------|
| `iso_reqpay.bin` | 0200 | ReqPay – fund transfer |
| `iso_reqchktxn.bin` | 0200 | ReqChkTxn – check transaction |
| `iso_reqhbt.bin` | 0800 | ReqHbt – heartbeat |
| `iso_reqlistaccpvd.bin` | 0200 | ReqListAccPvd – list providers |
| `iso_reqvaladd.bin` | 0200 | ReqValAdd – name enquiry |

**In Postman:** Body → Binary → Select file (e.g. `iso_reqpay.bin`).  
Generated by **mock_switch** `SampleIsoGenerator` (MockIsoPackager matches IMPS Backend for compatibility).
